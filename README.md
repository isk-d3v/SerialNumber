# macOs
---

```bash
brew install dotnet
```
```bash
dotnet new console -n SerialInfo
cd SerialInfo
cp /SerialInfo_macOS.cs Program.cs
dotnet run
```

---
# Linux 
---

```bash
sudo apt update
sudo apt install -y dotnet-sdk-8.0
```

```bash
dotnet new console -n SerialInfo
cd SerialInfo
cp /SerialInfo_Linux.cs Program.cs
dotnet run
```
